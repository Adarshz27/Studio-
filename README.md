# Palette Studio — True Multi-Page Version

The landing page is intentionally limited to Home, About and Reviews. Services, Portfolio, Experience, Booking and Contact are separate HTML pages and are reached from the menu.

Change the WhatsApp destination in `script.js` with `WHATSAPP_NUMBER`. Use the country code, e.g. `918884986006`.
